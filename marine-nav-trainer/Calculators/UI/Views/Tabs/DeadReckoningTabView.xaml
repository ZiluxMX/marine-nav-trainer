<UserControl x:Class="marine_nav_trainer.Calculators.UI.Views.Tabs.DeadReckoningTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             MaxWidth="400"
             d:DesignHeight="450" Width="400">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,20,24,24">

            <!-- Pozycja startowa -->
            <ui:TextBlock Text="Pozycja statku" FontTypography="BodyStrong" Margin="0,0,0,10"/>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <ui:TextBlock Text="Szerokość (°)"/>
                    <ui:NumberBox x:Name="StartLat" SpinButtonPlacementMode="Hidden" Minimum="-90" Maximum="90"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <ui:TextBlock Text="Długość (°)"/>
                    <ui:NumberBox x:Name="StartLon" SpinButtonPlacementMode="Hidden" Minimum="-180" Maximum="180"/>
                </StackPanel>
            </Grid>

            <Separator Margin="0,0,0,5"/>

            <!-- Ruch -->
            <ui:TextBlock Text="Parametry ruchu" FontTypography="BodyStrong" Margin="0,0,0,10"/>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <ui:TextBlock Text="KdD (°)"/>
                    <ui:NumberBox x:Name="Kdd" SpinButtonPlacementMode="Hidden" Minimum="0" Maximum="360"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <ui:TextBlock Text="Droga (Nm)"/>
                    <ui:NumberBox x:Name="DistanceNm" SpinButtonPlacementMode="Hidden" Minimum="0" Maximum="9999"/>
                </StackPanel>
            </Grid>

            <ui:Button Content="Oblicz" Click="Oblicz_Click" HorizontalAlignment="Right" Margin="0,0,0,20"/>

            <Separator Margin="0,0,0,5"/>

            <!-- Wyniki -->
            <ui:TextBlock Text="Wyniki — pozycja zliczona" FontTypography="BodyStrong" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <ui:TextBlock Text="Szerokość (°)"/>
                    <ui:NumberBox x:Name="DeadReckoningLat" SpinButtonPlacementMode="Hidden" Minimum="-90" Maximum="90"/>
                    <ui:TextBlock x:Name="DeadReckoningLatDegMin" Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <ui:TextBlock Text="Długość (°)"/>
                    <ui:NumberBox x:Name="DeadReckoningLon" SpinButtonPlacementMode="Hidden" Minimum="-180" Maximum="180"/>
                    <ui:TextBlock x:Name="DeadReckoningLonDegMin" Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontSize="11"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
